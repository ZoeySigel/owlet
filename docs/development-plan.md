# 分阶段进度

更新：2026-09-07。已完成云端部署及根域名 HTTPS，真实模型仍待完成。

| 阶段 | 已实现与证据 | 剩余验收 |
|---|---|---|
| 1 基础与账号 | React / Go / PostgreSQL、初始迁移、邀请码注册、会话、管理员功能；云端隔离与公网 HTTPS Cookie 检查通过 | 长期使用观察 |
| 2 手动创作 | 商品/品牌、快照、三套模板、四页编辑、裁剪、草稿、复制、PNG/ZIP；浏览器完整流程通过 | 不同真实商品图片的人工试用 |
| 3 任务费用 | 持久队列、Worker、SSE、幂等、预留结算、取消、全站暂停；云端并发测试及实际服务重启通过 | 真实付费对账 |
| 4 真实 AI | BigModel 文本流式和图片适配代码、局部重写、共享/单页背景、费用展示 | 账号权限、费率、实际 usage 和图片效果均未验证；当前仅 mock |
| 5 可靠性历史 | 未知结果保留预留、下载恢复、版本恢复、30 天清理及共享保护、后台核对；数据库测试通过 | 实际上游故障、长时间运行验证 |
| 6 部署投递 | systemd/Caddy 实际部署、公网 HTTPS 验收、云端三链路、独立备份恢复、定时备份/监测、日志限制、首次加密异机备份、演示材料 | 整机重启、持续负载监测、自动异机备份 |

真实付费开关保持关闭。BigModel 配置未就绪，长期运维与外部告警仍待完善，所以不能把阶段 4、6 宣称为全部验收完成。

## 下一次执行顺序

1. 根域名 https://owl-et.me 已部署；清理 Namecheap 中重复的 www DNS 记录。
2. 使用公开演示地址试用真实商品资料，记录用户体验问题。
3. 核实 BigModel 通用 API 账号模型、费率及上下文上界，将密钥仅写入服务器环境；所有未结算 mock 任务处理完后使用独立正式数据库。
4. 小额验证一篇文本和一张背景，核对供应商账单与本地流水。真实调用计入 5 元/天、50 元/月。
5. 接入自动异机备份与外部告警，择机验证整台主机重启和持续负载，再更新真实记录与简历表述。
